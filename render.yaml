services:
  - type: web
    name: rand-real-estate
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: node server/production-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true