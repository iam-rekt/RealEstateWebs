# Application Environment
NODE_ENV=production

# Session Configuration (generate a secure random string)
SESSION_SECRET=your-super-secure-session-secret-here

# CRITICAL SECURITY: Admin Credentials
# Change these immediately for production deployment!
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-password-here
ADMIN_EMAIL=admin@yourcompany.com

# HTTPS Configuration (for production)
FORCE_HTTPS=true

# Note: No database configuration needed - all data is stored in memory
# This makes deployment simple but data will reset on each deployment

# Optional: For external image storage (if using cloud storage)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret